# Patterns for git-secrets to detect
# API keys (only with actual values, not variable assignments)
OPENAI_API_KEY=[A-Za-z0-9_-]+
GEMINI_API_KEY=[A-Za-z0-9_-]+
API_KEY=[A-Za-z0-9_-]+
# Folder and Script IDs (only with actual values)
FOLDER_ID=[A-Za-z0-9_-]+
SCRIPT_ID=[A-Za-z0-9_-]+
# OpenAI API keys
sk-[A-Za-z0-9]{48}
# Credentials (only with actual values, not variable assignments)
password=\".*\"
password='.*'
token=\".*\"
token='.*'
secret=\".*\"
secret='.*'
credentials=\".*\"
credentials='.*'
